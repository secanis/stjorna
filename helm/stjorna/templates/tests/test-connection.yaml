apiVersion: v1
kind: Pod
metadata:
  name: {{ include "stjorna.fullname" . }}-test-connection
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "stjorna.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox:1.36
      command: ["sh", "-c"]
      args:
        - |
          set -e
          URL="http://{{ include "stjorna.pocketbase.fullname" . }}.{{ .Values.namespace.name }}.svc.cluster.local:{{ .Values.pocketbase.service.port }}{{ .Values.pocketbase.healthcheck.path }}"
          echo "Checking $URL"
          wget -q --spider "$URL"
          echo "OK"
