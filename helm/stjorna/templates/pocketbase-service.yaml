apiVersion: v1
kind: Service
metadata:
  name: {{ include "stjorna.pocketbase.fullname" . }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "stjorna.labels" . | nindent 4 }}
    app.kubernetes.io/component: pocketbase
spec:
  type: {{ .Values.pocketbase.service.type }}
  ports:
    - name: http
      port: {{ .Values.pocketbase.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "stjorna.pocketbase.selectorLabels" . | nindent 4 }}
