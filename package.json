{
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  },
  "scripts": {
    "test:e2e": "npm run build --prefix frontend && npx playwright test",
    "test:e2e:fast": "npx playwright test",
    "test:frontend": "cd pocketbase/test && npx vitest run ../../frontend/src/utils/mediaUrl.test.ts",
    "test:unit": "cd pocketbase/test && npx vitest run ../../frontend/src/utils/"
  },
  "dependencies": {
    "pocketbase": "^0.27.0"
  }
}
